import { RemoveNumsPipe } from './remove-nums.pipe';

describe('RemoveNumsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveNumsPipe();
    expect(pipe).toBeTruthy();
  });
});
